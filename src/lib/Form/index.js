import styled from "styled-components";

//The form loading that we aren't using yet.
export const FormLoading = styled.img.attrs(() => ({
  src: "../../../../public/img/dots.svg",
  alt: "Esperando..."
}))`
  width: 4rem;
`;
